#include<stdio.h>
int main()
{
	    int i;
		char s[6];
		printf("Enter your name\n");
	    gets(s);
	//	for(i=0;i<=9;i++)
	//	   printf("%s",s[4]);
	    puts(s);
	    printf("%s",s);
	    getch();
		return 0;
		
}
