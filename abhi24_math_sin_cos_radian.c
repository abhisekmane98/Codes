//
#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
		float a,b;
		printf("enter value of 0 in radians");   /*as sin function or any trigo function accept value in radians only
		                                            but it run ??? why  if you enter 30 then it assume as radian and print 
		                                            resprctive value rather than 1/2
		scanf("%f",&a);                         
		b=sin(a);
		printf("%f",b);
		return 0;
		
}
