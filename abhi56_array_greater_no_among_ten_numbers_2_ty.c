// greater No. among 10 NUmbers
// array
// trying to solve without storing all 10 numbers
#include<stdio.h>
int main()
{
		int a[10],i,j;
		for(i=0;i<=9;i++)
		{
	   		scanf("%d",&a[i]);
	   		if(a[i+1]>=a[i])
	   		  continue;
	   		else
	   		   a[i-1]=j;
	   		   i
	   		  
	    }
	    printf("%d is greatest No.",a[i]);
