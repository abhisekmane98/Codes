// No. divisible by 7 or not
#include<stdio.h>
#include<conio.h>
int main()
{
		int a;
		printf("Enter a Number");
		scanf("%d",&a);
		if(a%17==0)
		   printf("No. divisible by 17");
		else
		   printf("No. is non divisible by 7");
		return 0;
}
